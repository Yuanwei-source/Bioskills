# 经验学习政策

案例事实可以自动保存和检索；候选 lesson 不能自动修改 `SKILL.md` 或硬规则。
`candidate → verified` 需要明确验证标准、支持案例、反例审查和版本记录；矛盾经验并存，不通过频次投票抹除。
原始 FASTQ/BAM 默认只记录路径和 SHA-256，不复制进公共仓库。候选经验发布应生成 promotion proposal，并支持撤销而不删除历史证据。
