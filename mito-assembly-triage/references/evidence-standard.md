# 证据标准

- FASTA/注释可以证明文件内容和注释内部一致性，不能证明原始测序支持。
- 参考相似度、基因顺序和单软件结果只能作为定位线索；不能单独证明样本真实序列或结构。
- raw-read-supported 必须有样本 reads、比对参数、MAPQ/碱基质量和定位结果记录。
- 序列修改和注释修改分开保存；候选文件不得覆盖原始文件。
- 证据冲突、重复无法唯一解析、缺少关键输入时，状态为 `UNRESOLVED`，置信度为 `low` 或 `not_assessable`。
