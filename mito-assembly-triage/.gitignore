logs/
__pycache__/
knowledge/cases/
# 本地送审/评审材料，不上传远程仓库
references-review/
# 未发表测序数据（符号链接指向实验机/NAS）。
# 注意: 这里**不能**写尾部斜杠 —— 尾部斜杠只匹配目录，而 git 把这些本地数据记成的
# 是**符号链接**（blob 内容是链接目标文本），会把 NAS 路径与样本编号写进公共仓库。
# 不带斜杠同时覆盖同名目录与同名符号链接/文件。
tests/tests_data
